# Describes the runners that the CI system can use

ubuntu-22.04:
  arch: x86_64
  platform: linux
  free: true

ubuntu-22.04-arm:
  arch: aarch64
  platform: linux
  free: true

macos-latest:
  arch: x86_64
  platform: darwin
  free: true

ubuntu-latest:
  arch: x86_64
  platform: linux
  free: true

macos-latest:
  arch: x86_64
  platform: darwin
  free: true

windows-latest:
  arch: x86_64
  platform: windows
  free: true
